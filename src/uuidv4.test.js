describe('uuidV4', () => {
  it.todo('should return an array with a string by default')

  it.todo('should return an string whose pattern matches the uuidv4 spec')

  it.todo('should return an array of strings if a quantity param is specified')

  it.todo('should return an error if the quantity param is not valid')
})
